import unittest
import collections
from homework import DequeContainer

class TestDequeContainer (unittest.TestCase):
    
    def _init_(self):
        self.test_deque = collections.deque()
        
    def test_area(self)
        self.assertEqual(deque.this_append(20), self.test_deque.append(20))
        self.assertEqual(deque.this_append(0), self.test_deque.append(0))
        self.assertEqual(deque.this_append(220), self.test_deque.append(220))
